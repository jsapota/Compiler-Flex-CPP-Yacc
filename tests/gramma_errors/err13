{
    ERROR!  syntax error    ELSE
    LINE: 39
    ELSE
}

VAR

a b c d e f g

BEGIN

a := 0;
b := 0;
c := 0;
d := 0;
e := 0;
f := 0;
g := 0;

IF a < b THEN
    IF b < c THEN
        IF d < e THEN
            FORiFROMbTOcDO
                SKIP;
            ENDFOR
        ELSE
            IF e < f THEN
                IFf<gTHEN
                    WHILEa<bDO
                        SKIP;
                    ENDWHILE
                ELSE
                    SKIP;
                ENDIF
            ELSE
                SKIP;
            ENDIF
    ELSE
        SKIP;
    ENDIF
ELSE
    SKIP;
ENDIF

END
